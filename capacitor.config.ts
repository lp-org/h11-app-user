import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'blockchain-ionic',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
